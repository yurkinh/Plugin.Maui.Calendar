<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SampleApp.Views.XamlSourcePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    Title="XAML"
    ios:Page.ModalPresentationStyle="PageSheet">
    <Grid RowDefinitions="Auto,*">
        <Grid
            Padding="20,16,12,4"
            ColumnDefinitions="*,Auto,Auto"
            ColumnSpacing="4">
            <VerticalStackLayout Spacing="2" VerticalOptions="Center">
                <Label Style="{StaticResource TitleLabelStyle}" Text="XAML" />
                <Label x:Name="SourceLabel" Style="{StaticResource CaptionLabelStyle}" />
            </VerticalStackLayout>
            <Button
                x:Name="CopyButton"
                Grid.Column="1"
                Clicked="OnCopyClicked"
                IsEnabled="False"
                Style="{StaticResource TextButtonStyle}"
                Text="Copy" />
            <Button
                Grid.Column="2"
                Clicked="OnCloseClicked"
                SemanticProperties.Description="Close"
                Style="{StaticResource IconButtonStyle}"
                Text="&#xf00d;" />
        </Grid>

        <Border
            Grid.Row="1"
            Margin="16,8,16,16"
            Padding="0"
            BackgroundColor="{toolkit:AppThemeResource CodeBackgroundColor}"
            StrokeShape="RoundRectangle 12"
            StrokeThickness="0">
            <!--  Both directions: the label gets unlimited width, so long lines scroll instead of wrapping  -->
            <ScrollView Orientation="Both">
                <Label
                    x:Name="CodeLabel"
                    Padding="16"
                    FontFamily="{OnPlatform iOS=Menlo, MacCatalyst=Menlo, Android=monospace, WinUI=Consolas}"
                    FontSize="12"
                    LineBreakMode="WordWrap" />
            </ScrollView>
        </Border>
    </Grid>
</ContentPage>
