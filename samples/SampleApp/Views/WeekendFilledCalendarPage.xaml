<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SampleApp.Views.WeekendFilledCalendarPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:SampleApp.Controls"
    xmlns:local="clr-namespace:SampleApp.ViewModels"
    xmlns:plugin="clr-namespace:Plugin.Maui.Calendar.Controls;assembly=Plugin.Maui.Calendar"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:Name="weekendFilledCalendarPage"
    Title="Weekend Filled Calendar"
    x:DataType="local:WeekendFilledCalendarPageViewModel"
    Shell.TabBarIsVisible="False">
    <ContentPage.ToolbarItems>
        <controls:XamlSourceToolbarItem />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="0,8" Spacing="0">

            <plugin:Calendar
                x:Name="calendar"
                Margin="8,4"
                Padding="0"
                DayViewCornerRadius="5"
                DaysTitleLabelFirstUpperRestLower="True"
                EventIndicatorType="BottomDot"
                Events="{Binding Events}"
                EventsScrollViewVisible="False"
                FooterSectionVisible="False"
                SelectedDate="{Binding SelectedDate}"
                EventIndicatorSelectedTextColor="{toolkit:AppThemeResource TextPrimaryColor}"
                SelectedDayBackgroundColor="{toolkit:AppThemeResource AccentSoftColor}"
                SelectedDayTextColor="{toolkit:AppThemeResource TextPrimaryColor}"
                SelectedTodayTextColor="{toolkit:AppThemeResource AccentColor}"
                Style="{StaticResource SampleCalendarStyle}"
                WeekendDayBackgroundColor="{toolkit:AppThemeResource SurfaceVariantColor}"
                WeekendTitleStyle="{StaticResource SampleDaysTitleLabelStyle}">

                <plugin:Calendar.HeaderSectionTemplate>
                    <DataTemplate>
                        <Grid
                            Padding="8,6,8,10"
                            x:DataType="plugin:Calendar"
                            ColumnDefinitions="44,*,44">
                            <Button
                                Grid.Column="0"
                                Padding="0"
                                BackgroundColor="Transparent"
                                BorderWidth="0"
                                Command="{Binding PrevLayoutUnitCommand}"
                                FontSize="26"
                                Text="‹"
                                TextColor="{toolkit:AppThemeResource TextPrimaryColor}" />
                            <HorizontalStackLayout
                                Grid.Column="1"
                                HorizontalOptions="Center"
                                Spacing="6"
                                VerticalOptions="Center">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="18"
                                    Text="{Binding LayoutUnitText}"
                                    VerticalOptions="Center" />
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="18"
                                    Text="{Binding LocalizedYear}"
                                    VerticalOptions="Center" />
                            </HorizontalStackLayout>
                            <Button
                                Grid.Column="2"
                                Padding="0"
                                BackgroundColor="Transparent"
                                BorderWidth="0"
                                Command="{Binding NextLayoutUnitCommand}"
                                FontSize="26"
                                Text="›"
                                TextColor="{toolkit:AppThemeResource TextPrimaryColor}" />
                        </Grid>
                    </DataTemplate>
                </plugin:Calendar.HeaderSectionTemplate>

            </plugin:Calendar>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
