<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="SampleApp.Controls.CalendarFooter"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:plugin="clr-namespace:Plugin.Maui.Calendar.Controls;assembly=Plugin.Maui.Calendar"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    x:DataType="plugin:Calendar">
    <Grid
        Padding="4,0"
        ColumnDefinitions="*,Auto"
        HeightRequest="44">
        <Label Style="{Binding SelectedDateLabelStyle}" Text="{Binding SelectedDateText}" />

        <Label
            Grid.Column="1"
            Margin="0,0,8,0"
            FontFamily="FontAwesomeSolid"
            FontSize="14"
            IsVisible="{Binding FooterArrowVisible}"
            Text="&#xf077;"
            TextColor="{toolkit:AppThemeResource TextSecondaryColor}"
            VerticalOptions="Center">
            <Label.Triggers>
                <DataTrigger
                    Binding="{Binding CalendarSectionShown}"
                    TargetType="Label"
                    Value="False">
                    <Setter Property="Text" Value="&#xf078;" />
                </DataTrigger>
            </Label.Triggers>
        </Label>
        <Grid.GestureRecognizers>
            <TapGestureRecognizer Command="{Binding ShowHideCalendarCommand}" />
        </Grid.GestureRecognizers>
    </Grid>
</ContentView>
